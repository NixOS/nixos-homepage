---
import Link from '../util/Link.astro';

interface Props {
  href?: string;
  classList?: string[];
}

const { href = '#', classList = [] } = Astro.props;
---

<span class="mx-[0.5rem] hidden md:inline">
  <Link variant="blue" href={href} class:list={[classList]}>
    <slot />
  </Link>
</span>
