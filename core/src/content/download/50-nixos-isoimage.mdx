---
title: ISO image
family: nixos
platform: iso
---

import Button from '../../components/ui/Button.astro';
import Tag from '../../components/ui/Tag.astro';
import { nixosDownloadLink } from '../../lib/links';

You can install NixOS on physical hardware by burning one of the CD images onto a blank CD/DVD disk, or [by copying it onto a USB stick](/manual/nixos/stable#sec-booting-from-usb). For **installation instructions**, please see the [manual](/manual/nixos/stable).

### Graphical ISO image

The graphical installation ISO image **contains the graphical NixOS installer** as well as several **Desktop Environments** and several applications. It’s a _live CD_, so it allows you to get an impression of NixOS (and the Nix package manager) without installing it.

<div class="flex flex-col gap-4 md:gap-6 my-4">
  <span class="flex items-center gap-2 lg:block">
    <Button
      color="semidarkblue"
      href={nixosDownloadLink("graphical", "x86_64-linux", "iso", false)}
      shaHref={nixosDownloadLink("graphical", "x86_64-linux", "iso", true)}
      shaText="(SHA-256)"
      size="sm-mobfull"
      classList = {["w-full", "md:w-auto", "block", "md:inline", "text-center", "md:text-left"]}
      label="Download (Graphical, 64-bit Intel/AMD)"
      />
  </span>

{" "}

<span>
  <Button
    color="semidarkblue"
    href={nixosDownloadLink('graphical', 'aarch64-linux', 'iso', false)}
    shaHref={nixosDownloadLink('graphical', 'aarch64-linux', 'iso', true)}
    shaText="(SHA-256)"
    size="sm-mobfull"
    classList={[
      'w-full',
      'md:w-auto',
      'block',
      'md:inline',
      'text-center',
      'md:text-left',
    ]}
    label="Download (Graphical, 64-bit ARM)"
  />
</span>

{" "}
</div>

**You can choose the desktop environment to boot with in the boot menu.** Note that the desktop environment you choose for your live environment does not affect your resulting installation. The included installer also allows installing various other desktops, or installing without a desktop.

### Minimal ISO image

The minimal installation ISO image **does not contain the graphical user interface,** and is therefore a lot smaller. You have to [run the installer from the console](/manual/nixos/stable/#ch-installation). It contains a number of rescue tools.

<div class="flex flex-col gap-4 md:gap-6 my-4">
  <span>
      <Button
      color="semidarkblue"
      href={nixosDownloadLink("minimal", "x86_64-linux", "iso", false)}
      shaHref={nixosDownloadLink("minimal", "x86_64-linux", "iso", true)}
      shaText="(SHA-256)"
      size="sm-mobfull"
      classList = {["w-full", "md:w-auto", "block", "md:inline", "text-center", "md:text-left"]}
      label="Download (64-bit Intel/AMD)"
      />
  </span>

  <span>
    <Button
      color="semidarkblue"
      href={nixosDownloadLink("minimal", "aarch64-linux", "iso", false)}
      shaHref={nixosDownloadLink("minimal", "aarch64-linux", "iso", true)}
      shaText="(SHA-256)"
      size="sm-mobfull"
      classList={[
        "w-full",
        "md:w-auto",
        "block",
        "md:inline",
        "text-center",
        "md:text-left",
      ]}
      label="Download (64-bit ARM)"
      />
  </span>
</div>
