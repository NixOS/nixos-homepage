---
import type { HTMLTag } from 'astro/types';

interface Props {
  as?: HTMLTag;
}

const { as: Element = 'div', class: extraClasses, ...props } = Astro.props;
---

<Element class:list={['shadow-lg rounded-lg p-4', extraClasses]} {...props}>
  <slot />
</Element>
