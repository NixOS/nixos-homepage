---
title: Windows (WSL2)
family: nix
platform: windows
---

import CodeBlock from "../../components/layout/CodeBlock.astro";

### Multi-user installation (Requires WSL with systemd enabled)

WSL versions 0.67.6 and above has systemd support.
Follow
[Microsoft's systemd guide](https://devblogs.microsoft.com/commandline/systemd-support-is-now-available-in-wsl)
to configure it, and then install Nix using:

```bash
$ sh <(curl -L https://releases.nixos.org/nix/nix-2.18.5/install) --daemon
```

### Single-user installation

Install Nix via the [single-user installation](/manual/nix/stable/installation/single-user)

```bash
$ sh <(curl -L https://releases.nixos.org/nix/nix-2.18.5/install) --no-daemon
```
