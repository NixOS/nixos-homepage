---
title: Graphical ISO image
family: nixos
platform: graphical
---

import Button from '../../components/ui/Button.astro';
import Tag from '../../components/ui/Tag.astro';
import { nixosDownloadLink } from '../../lib/links';

You can install NixOS on physical hardware by burning one of the CD images onto a blank CD/DVD disk, or [by copying it onto a USB stick](/manual/nixos/stable#sec-booting-from-usb). For **installation instructions**, please see the [manual](/manual/nixos/stable).

The graphical installation ISO image **contains the graphical NixOS installer** as well as a **Desktop Environment** and several applications. It’s a _live CD_, so it allows you to get an impression of NixOS (and the Nix package manager) without installing it.

Note that the installer allows installing various desktops, regardless of which desktop image has been booted. The stable image comes in GNOME and Plasma variations, while the unstable image includes both as options in the boot menu.

### Stable

#### GNOME

<div class="flex flex-col gap-4 md:gap-6 my-4">
  <span class="flex items-center gap-2 lg:block">
    <Button
      color="semidarkblue"
      href={nixosDownloadLink("gnome", "x86_64-linux", "iso", false)}
      shaHref={nixosDownloadLink("gnome", "x86_64-linux", "iso", true)}
      shaText="(SHA-256)"
      size="sm-mobfull"
      classList = {["w-full", "md:w-auto", "block", "md:inline", "text-center", "md:text-left"]}
      label="Download (GNOME, 64-bit Intel/AMD)"
      />
    <Tag color="darkgreen" classList={["hidden", "md:inline"]}>Recommended</Tag>
  </span>

{" "}

<span>
  <Button
    color="semidarkblue"
    href={nixosDownloadLink('gnome', 'aarch64-linux', 'iso', false)}
    shaHref={nixosDownloadLink('gnome', 'aarch64-linux', 'iso', true)}
    shaText="(SHA-256)"
    size="sm-mobfull"
    classList={[
      'w-full',
      'md:w-auto',
      'block',
      'md:inline',
      'text-center',
      'md:text-left',
    ]}
    label="Download (GNOME, 64-bit ARM)"
  />
</span>

{" "}

#### Plasma Desktop

<span>
  <Button
    color="semidarkblue"
    href={nixosDownloadLink('plasma6', 'x86_64-linux', 'iso', false)}
    shaHref={nixosDownloadLink('plasma6', 'x86_64-linux', 'iso', true)}
    shaText="(SHA-256)"
    size="sm-mobfull"
    classList={[
      'w-full',
      'md:w-auto',
      'block',
      'md:inline',
      'text-center',
      'md:text-left',
    ]}
    label="Download (Plasma Desktop, 64-bit Intel/AMD)"
  />
</span>

<span>
  <Button
    color="semidarkblue"
    href={nixosDownloadLink('plasma6', 'aarch64-linux', 'iso', false)}
    shaHref={nixosDownloadLink('plasma6', 'aarch64-linux', 'iso', true)}
    shaText="(SHA-256)"
    size="sm-mobfull"
    classList={[
      'w-full',
      'md:w-auto',
      'block',
      'md:inline',
      'text-center',
      'md:text-left',
    ]}
    label="Download (Plasma Desktop, 64-bit ARM)"
  />
</span>

### Unstable

<span>
  <Button
    color="semidarkblue"
    href={nixosDownloadLink(
      'graphical',
      'x86_64-linux',
      'iso',
      false,
      'unstable',
    )}
    shaHref={nixosDownloadLink(
      'graphical',
      'x86_64-linux',
      'iso',
      true,
      'unstable',
    )}
    shaText="(SHA-256)"
    size="sm-mobfull"
    classList={[
      'w-full',
      'md:w-auto',
      'block',
      'md:inline',
      'text-center',
      'md:text-left',
    ]}
    label="Download (Graphical, 64-bit Intel/AMD)"
  />
</span>

  <span>
    <Button
      color="semidarkblue"
      href={nixosDownloadLink("graphical", "aarch64-linux", "iso", false, "unstable")}
      shaHref={nixosDownloadLink("graphical", "aarch64-linux", "iso", true, "unstable")}
      shaText="(SHA-256)"
      size="sm-mobfull"
      classList = {["w-full", "md:w-auto", "block", "md:inline", "text-center", "md:text-left"]}
      label="Download (Graphical, 64-bit ARM)"
      />
  </span>
</div>
