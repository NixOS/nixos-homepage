---
title: VirtualBox
family: nixos
platform: virtualbox
---
import Button from "../../components/ui/Button.astro";
import CodeInline from "../../components/layout/CodeInline.astro";

<div class="bg-nixlightorange text-nixdarkerorange p-6 rounded-2xl mb-[1rem]">
  <span class="font-bold">
    This is a demo appliance
  </span>
  for VirtualBox (in OVA format).
</div>

VirtualBox image includes the **Plasma Desktop** (was KDE) as well as **the VirtualBox guest additions.**

<div class="my-[1rem]">
  <Button
    color="semidarkblue"
    href="https://channels.nixos.org/nixos-23.11/latest-nixos-x86_64-linux.ova"
    shaHref="https://channels.nixos.org/nixos-23.11/latest-nixos-x86_64-linux.ova.sha256"
    shaText="(SHA-256)"
    classList = {["w-full", "md:w-auto", "block", "md:inline", "text-center", "md:text-left"]}
  >Download (64-bit)</Button>
</div>

### To use it

- Download the OVA file using the button above.
- Open VirtualBox.
- Run `File → Import Appliance` from the menu.
- Select previously downloaded OVA file.
- Click `Import`.
- You can then start the virtual machine.
- You can log in as **user** `demo`, **password** `demo`.
- To obtain a root shell, run `sudo -i` in the terminal (`konsole`).
